/*@import "twitter/bootstrap/bootstrap";*/
/*=require twitter-bootstrap-static/bootstrap*/
/*

  Use Font Awesome icons (default)
  To use Glyphicons sprites instead of Font Awesome, replace with "require twitter-bootstrap-static/sprites"
  =require twitter-bootstrap-static/fontawesome
  */

@navbarHeight: 41px;
.space0 { margin-bottom: 6.25px; }
.space1 { margin-bottom: 12.5px; }
.space2 { margin-bottom: 25px; }
.space3 { margin-bottom: 37.5px; }
.space4 { margin-bottom: 50px; }
.rightspace2 {margin-right: 12.5px; }

.navbar {
  opacity: .8;
}

.navbar .brand {
  margin-left: -70px;
  position: absolute;
  left: 50%;
  padding: 20px 90px;
  background-image: url(<%= asset_path 'tripblink_logo.png' %>);
}
body {
  background-color: #D3D3D3;
   /*background-image: #ffffff url(<%= asset_path 'blue_hatch.jpg' %>);*/
}
.container.main {
  margin-top: 60px;
}

.center {
  text-align: center;
}

ul, ol {
  list-style: none;
  margin: 0;
}

li {
  list-style: none;
}

ul > .item {
  border: 1px solid #e7e7e7;
  border-radius: 3px;
  width: 270px;
  height: 260px;
  background-color: #ffffff;
  margin-bottom: 6.25px;
  margin-right: 12.5px;
}

.item img {
  border-bottom: 1px solid #e7e7e7;
  width: 270px;
  height: 210px;
  background-color: grey;
}

.name
  a {
    text-decoration: none;
    color: black;
  }
  a:hover {
    text-decoration: none;
  }
}

.search_bar .vertical-line {
  border-right: 2px solid #ed9f95;
  display: inline-block;
  height: 10px;
  margin-top: 10px;
  width: 1px;
}

footer {
  bottom: 0;
}
#filters {
  display: inline-block;
}
#filters li, #sort-by li  {
  display: inline-block;
}

.search_bar {
  background-color: #ffb7b7;
  padding: 10px auto;

  /*margin-top: 50px;*/
  /*width: auto;*/

}
#options ul {
  display: inline-block;

}
.search_bar #options {
  padding: 10px;
}
/*.hoverMask {
  background: rgba(255,255,255,0.1)
}*/

.fadeContainer {
  /*-webkit-transition: opacity 0.42s ease-out;*/
  /*transition: opacity 0.42s ease-out;*/
  opacity: .8;
}
/**** Isotope Filtering ****/

.isotope-item {
  z-index: 2;
}

.isotope-hidden.isotope-item {
  pointer-events: none;
  z-index: 1;
}

/**** Isotope CSS3 transitions ****/

.isotope,
.isotope .isotope-item {
  -webkit-transition-duration: 0.8s;
     -moz-transition-duration: 0.8s;
      -ms-transition-duration: 0.8s;
       -o-transition-duration: 0.8s;
          transition-duration: 0.8s;
}

.isotope {
  -webkit-transition-property: height, width;
     -moz-transition-property: height, width;
      -ms-transition-property: height, width;
       -o-transition-property: height, width;
          transition-property: height, width;
}

.isotope .isotope-item {
  -webkit-transition-property: -webkit-transform, opacity;
     -moz-transition-property:    -moz-transform, opacity;
      -ms-transition-property:     -ms-transform, opacity;
       -o-transition-property:      -o-transform, opacity;
          transition-property:         transform, opacity;
}